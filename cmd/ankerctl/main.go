// Package main is the entry point for the ankerctl CLI and web server.
//
// ankerctl is a CLI and web UI tool for monitoring, controlling, and
// interfacing with AnkerMake M5 3D printers over MQTT and PPPP protocols.
package main

import (
	"fmt"
	"os"
)

func main() {
	// TODO: Initialize cobra root command and subcommands.
	fmt.Println("ankerctl (Go) - not yet implemented")
	os.Exit(0)
}
